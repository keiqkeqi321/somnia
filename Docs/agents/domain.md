# Domain Docs

The repository uses a single-context domain model.

Before relevant work, read `CONTEXT.md` and applicable ADRs under
`Docs/adr/` when they exist. Missing files require no action.

Use glossary terms consistently and explicitly surface conflicts with an
existing ADR. Domain files are created lazily when terminology or durable
architectural decisions are resolved.
